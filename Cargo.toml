[workspace]
members = ["trafford"]
resolver = "2"

# At the workspace root deliberately. A `[profile.release]` left in a member is
# silently ignored, and nobody notices until a release binary is three times the
# size it should be.
[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
