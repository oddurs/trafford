# Roadmap

A terminal knowledge base — an Obsidian-shaped markdown vault with modal editing, git, and a note-grounded assistant.

<!-- generated by cairn; edit the files in `cairn/items` and run `cairn render` -->

## v0.2 — Usable without the manual

`##########` 100% · 5 of 5 done · due 2026-09-20

The interface explains itself. A file tree instead of a flat list, the mouse

### done

- [x] `0001` Mouse support, and a right-click menu <sup>feature · p1 · mouse</sup>
- [x] `0002` Right-click every surface that has actions <sup>feature · p1 · mouse</sup>
- [x] `0003` Show the keyboard shortcut beside each menu entry <sup>feature · p2 · mouse</sup>
- [x] `0004` Open the context menu from the keyboard <sup>feature · p2 · mouse</sup>
- [x] `0005` Menus that are taller than the terminal <sup>bug · p2 · mouse</sup>

## v0.3 — Actions worth reaching for

`##########` 100% · 4 of 4 done · due 2026-10-15

The things you go to a menu *for*: moving and duplicating notes, folders,

### done

- [x] `0006` Entries that cannot apply should be shown, not hidden <sup>feature · p3 · mouse</sup>
- [x] `0007` Copy a note's link, path, or the selection <sup>feature · p2 · mouse</sup>
- [x] `0008` Move, duplicate, and make folders from the tree <sup>feature · p1 · tree</sup>
- [x] `0009` Act on a selection from the editor's menu <sup>feature · p2 · mouse</sup>

## v0.4 — What you wrote, as you wrote it

`##########` 100% · 8 of 8 done · due 2026-11-15

The editor stops showing markdown as plain text. Long lines wrap, links read as

### done

- [x] `0012` How much of the source should the editor render? <sup>spike · p0 · chrome</sup>
- [x] `0013` A model that maps source positions to screen positions <sup>feature · p0 · chrome</sup>
- [x] `0014` Soft-wrap long lines instead of scrolling sideways <sup>feature · p1 · editor</sup>
- [x] `0015` Show links as links, and the source where the cursor is <sup>feature · p1 · markdown</sup>
- [x] `0016` Follow a table-of-contents link to its heading <sup>feature · p2 · markdown</sup>
- [x] `0017` How does a rendered table get edited? <sup>spike · p1 · chrome</sup>
- [x] `0018` Draw markdown tables as tables <sup>feature · p2 · markdown</sup>
- [x] `0019` Retire the character-preserving invariant <sup>docs · p2 · docs</sup>

## v0.5 — Reading is its own posture

`#########·` 89% · 8 of 9 done · due 2026-12-20

Preview stops being the editor with the syntax hidden. A note's structure folds,

### backlog

- [ ] `0041` Say where you are in the note <sup>feature · p1 · chrome</sup>

### done

- [x] `0020` Fold a note's sections in the reading view <sup>feature · p0 · markdown</sup>
- [x] `0021` Show frontmatter as properties, not as seven lines of YAML <sup>feature · p1 · markdown</sup>
- [x] `0022` Draw callouts as callouts <sup>feature · p1 · markdown</sup>
- [x] `0023` Keep the current section's heading on screen <sup>feature · p1 · chrome</sup>
- [x] `0024` Peek at a link without leaving the note <sup>feature · p1 · chrome</sup>
- [x] `0025` Let the chrome recede while reading <sup>feature · p2 · chrome</sup>
- [x] `0039` Give preview a cursor of its own <sup>bug · p0 · chrome</sup>
- [x] `0040` Never land the cursor on a line that is folded away <sup>bug · p1 · chrome</sup>

## v1.0 — Safe to depend on

`########··` 75% · 15 of 20 done · due 2027-03-01

Documented, packaged, and hardened. Someone who is not the author can install

### backlog

- [ ] `0058` Paste a Ghostty theme and watch the site wear it <sup>feature · p2 · site</sup>
- [ ] `0059` Links to the site unfurl as something <sup>chore · p2 · site</sup>
- [ ] `0060` The roadmap is a page, built from the items <sup>feature · p2 · site</sup>
- [ ] `0061` Why trafford exists, as its own page <sup>docs · p2 · docs</sup>
- [ ] `0062` Moving between pages feels like one page <sup>chore · p3 · site</sup>

### done

- [x] `0042` What builds the site, and where does its code live? <sup>spike · p0 · site</sup>
- [x] `0043` Make the crate a workspace so site tooling never ships in the binary <sup>chore · p0 · packaging</sup>
- [x] `0044` Build the site from a directory of notes, deterministically <sup>feature · p0 · site</sup>
- [x] `0045` The development server binds port zero <sup>feature · p0 · site</sup>
- [x] `0046` Reload the open page on save, over one connection <sup>feature · p1 · site</sup>
- [x] `0047` Generate the site's palette from the app's themes <sup>chore · p1 · theme</sup>
- [x] `0048` Every screenshot is generated by the probe, never pasted in <sup>chore · p1 · docs</sup>
- [x] `0049` CI gates the site the way it gates the crate <sup>chore · p1 · site</sup>
- [x] `0050` Deploy from CI, and from nowhere else <sup>chore · p0 · packaging</sup>
- [x] `0051` Publish rustdoc under /api as part of the same build <sup>chore · p2 · docs</sup>
- [x] `0052` Smoke-test the site by serving it on a random port <sup>chore · p1 · site</sup>
- [x] `0053` The landing page is one screen, and the screenshot is real <sup>feature · p1 · site</sup>
- [x] `0055` Give the site a typeface <sup>chore · p1 · site</sup>
- [x] `0056` The landing page has a grid, not a column <sup>feature · p1 · site</sup>
- [x] `0057` The hero plays, and it is the real program <sup>feature · p1 · site</sup>

## later — Someday

`##········` 11% · 1 of 9 done

Good ideas without a date yet.

### backlog

- [ ] `0026` A rail showing the shape of the note beside it <sup>feature · p2 · chrome</sup>
- [ ] `0027` Every unfinished task in the vault, in one place <sup>feature · p2 · vault</sup>
- [ ] `0028` Show where you came from, and reopen where you left off <sup>feature · p3 · chrome</sup>
- [ ] `0029` Read two notes side by side <sup>feature · p3 · chrome</sup>
- [ ] `0030` Should the editor render in place, the way Obsidian does? <sup>spike · p2 · editor</sup>
- [ ] `0031` Inline images and dataview, and why not yet <sup>docs · p3 · docs</sup>
- [ ] `0054` Search the docs from an index the build produces <sup>feature · p2 · site</sup>
- [ ] `0063` trafford, running in the browser <sup>feature · p2 · site</sup>

### done

- [x] `0011` Open a note in an external application <sup>feature · p3 · mouse</sup>

